import React from 'react'
import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const CameraCapture = () => {
  return (
    <SafeAreaView>
      <Text className='text-green-500 text-2xl'>CameraCapture</Text>
    </SafeAreaView>
  )
}

export default CameraCapture